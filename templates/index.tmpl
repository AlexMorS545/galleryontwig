<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="../css/style.css">
  <title>{{ title }}</title>
</head>
<body>
  <h1>{{ title }}</h1>
  <div class="gallery-wrp">
    {% for img in images %}
      <a href="photo.php?img={{ img }}" class="gallery-link"><img class="gallery-img" src="{{ image_src }}/{{ img }}" alt="photo"></a>
    {% endfor %}
  </div>
</body>
</html>
